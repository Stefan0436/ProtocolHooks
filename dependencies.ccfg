# CCFG document for the mod.artifacts.deps server file

group> 'protocol'
modid> 'hooks'

# Dependency version statement
dependencyVersion> '~=[0-9](\\.[0-9])+-(%gvs)(-build-[0-9]+)?'

artifacts> {
    org.asf.mods> {
        ProtocolHooks> '%mv'
    }
}
