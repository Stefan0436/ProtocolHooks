# CCFG document for the mod.artifacts.deps server file

group> 'protocol'
modid> 'hooks'

# Dependency version statement
dependencyVersion> '~=[0-9](\\.[0-9])+-(%gvs)'

artifacts> {
    org.asf.mods> {
        ProtocolHooks> '%mv'
    }
}
